# UrbanEye AI — Production environment
# NEXT_PUBLIC_API_URL is set in Vercel dashboard (not here)
# to avoid committing the real backend URL to git.
# This file only sets the demo mode default.
NEXT_PUBLIC_DEMO_MODE=true
