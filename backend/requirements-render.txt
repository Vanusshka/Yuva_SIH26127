# UrbanEye AI — Render.com deployment requirements
# Render uses Python 3.11 by default.
# PaddleOCR is NOT included here — too large for Render free tier (500MB disk limit).
# Set OCR_ENGINE=easyocr and MODEL_PROVIDER=huggingface on Render.

fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9
pydantic==2.7.4
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
numpy==1.26.4
opencv-python-headless==4.10.0.84
easyocr==1.7.1
ultralytics==8.2.87
httpx==0.27.0
